import React from "react";
import Thread from "../components/Thread";

const Feed = () => {
	return (
		<div className="feed">
			<main className="feed-main">
				<Thread />
			</main>
		</div>
	);
};

export default Feed;
